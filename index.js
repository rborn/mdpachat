import { AppRegistry } from 'react-native';
import App from '@src';

AppRegistry.registerComponent('mdpachat', () => App);

// This file in the root of the project should only contain the AppRegistry part and import the App from the src folder
